# LNR-022 Constitution Closeout — Watch Hub

- 日期：2026-09-13
- Task：`LNR-022`
- 执行者：OpenAI ChatGPT
- Baseline：`main@2b9cf6ba39d3306ec89557451207aa30d29a7cc9`
- Implementation head verified：`dd7f2dc61feb4ad8e570877a578aebb4134612b8`
- 状态：AUTOMATED GATE PASS / DEVICE TEST PENDING

## 1. Failure history（保留，不抹除）

### Failure A — test framework mismatch
- PR run：`34737178343`
- Architecture boundary gate：PASS
- Domain/Application step：FAIL
- Android tests / compile / APK：SKIPPED
- 原因：新 `WatchPortTest` 首版使用 JUnit4 `org.junit.*`，而 `core/application` 明确采用 `kotlin("test-junit5") + JUnit Platform`。
- 修复：改为 `kotlin.test.Test/assertEquals/assertFailsWith/assertTrue`。
- 修复 Commit：`dd7f2dc61feb4ad8e570877a578aebb4134612b8`

## 2. Successful verification

PR run：`34737240748` / run #509：
- Architecture boundary gate：PASS
- Domain + Application tests：PASS
- Android adapter unit tests：PASS
- Android debug compile：PASS
- Debug APK upload：PASS
- Workflow conclusion：SUCCESS

Android 真机外部 App 跳转、浏览器 fallback、悬浮窗权限往返：`NOT EXECUTED / WAITING EXTERNAL TEST`。

## 3. Post-change Compliance Review

### Architecture
PASS。
- `WatchPort` 位于 `core/application`，无 Android/Intent/PackageManager/Uri 等平台依赖。
- Android 平台实现集中于 `AndroidWatchPort`。
- Watch Adapter 不依赖 PRE/LIVE/POST Source Port、Provider 或 Arbitration Service。
- 打开直播入口不修改赛事事实 authority、source priority 或 canonical Match state。

### Global architecture / Region boundary
PASS。
- Bilibili、Huya、LoL Esports、YouTube、Twitch、X 进入同一 Catalog。
- `WatchRegion.MAINLAND/GLOBAL` 只用于 UI 展示分组。
- `launch(destinationId)` 不按 Region/赛区选择业务流程。
- 不存在 LPL/LCK/LEC/LCS 等赛区复制的 Watch 实现。

### Product parity / UI scope
PASS（LNR-022 范围）。
- Watch 入口继续位于既有 `RIFTSCREEN / 赛事副屏` 产品区域，而非另造赛区页或替换产品壳。
- 保留旧版 B站/虎牙快捷观赛，并补齐旧 baseline 已定义的 LoL Esports / YouTube / Twitch / X。
- 直播入口与赛事数据完全解耦的旧行为契约保留。
- Preflight 发现的遗留“检查临时 Riot Key”普通产品文案已改为 Provider-neutral 文案。

### Security / Android visibility
PASS（自动审查）。
- Manifest 仅声明六个目标客户端相关 package visibility；未使用 `QUERY_ALL_PACKAGES`。
- 无 Token/Key/Credential 新增或持久化。
- 唯一持久化状态为 pending destination id。

### Data / compatibility
PASS（自动范围）。
- 无赛事数据 Schema 变化。
- Watch pending state 不进入 Core Repository/Timeline。
- App/deep-link 可用性属于真实 Android 外部兼容性，保持 `WAITING EXTERNAL TEST`，没有冒充 CI PASS。

### Scope control
PASS。
- 未进入 Block 4 PRE/POST 剩余功能。
- 未修改赛事 Provider Arbitration。
- 未按赛区复制逻辑。

## 4. Functional status

`LIVE-031 / LIVE-032 / LIVE-033 / LIVE-034 = WAITING EXTERNAL TEST`。
原因：自动实现与 Gate 已通过，但完整 Definition of Done 仍要求 Android 真机外部应用/浏览器/权限往返证据。

## 5. Rollback

整体回滚点：`main@2b9cf6ba39d3306ec89557451207aa30d29a7cc9`。
Watch Port/Android Adapter/UI wiring 可独立回滚，不影响 PRE/LIVE/POST 事实模型。

## 6. Next gate

本 closeout commit 进入最终 exact-head PR Gate。只有该 Gate SUCCESS 并合并 main 后，才允许从最新 main 开始 Block 4 fresh Constitution Preflight。
