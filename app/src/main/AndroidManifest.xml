<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android 11+ package visibility: query only the streaming apps RiftLab can launch. -->
    <queries>
        <package android:name="tv.danmaku.bili" />
        <package android:name="com.huya.kiwi" />
        <package android:name="com.duowan.kiwi" />
        <package android:name="com.google.android.youtube" />
        <package android:name="tv.twitch.android.app" />
        <package android:name="com.twitter.android" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="bilibili" />
        </intent>
    </queries>

    <application
        android:name=".RiftLabApplication"
        android:icon="@drawable/ic_launcher_riftlab"
        android:roundIcon="@drawable/ic_launcher_riftlab"
        android:hardwareAccelerated="true"
        android:allowBackup="true"
        android:label="RiftLab"
        android:supportsRtl="true"
        android:theme="@style/Theme.RiftLab">

        <!-- LiteRT-LM GPU/OpenCL backend. Optional: unsupported devices fall back to CPU. -->
        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:configChanges="keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.EntityDetailActivity"
            android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="keyboardHidden|orientation|screenSize" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/riftlab_update_paths" />
        </provider>

        <service
            android:name=".overlay.RiftOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-started persistent esports match data overlay shown while watching a match in another app." />
        </service>

    </application>
</manifest>
